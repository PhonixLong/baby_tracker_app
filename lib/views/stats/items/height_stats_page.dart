import 'package:flutter/material.dart';
import 'single_stat_page.dart';

class HeightStatsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) =>
      SingleStatPage(type: '身高', color: Colors.blue);
}
