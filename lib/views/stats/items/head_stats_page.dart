import 'package:flutter/material.dart';
import 'single_stat_page.dart';

class HeadStatsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) =>
      SingleStatPage(type: '头围', color: Colors.deepPurple);
}
